{% extends baseTemplate %}


{% block form %}
    <h1>Импорт</h1>
    {{ form(form) }}
    <h2>Формат загрузки</h2>
    <table class="table">
        <tr>
            <th>
                #Столбец
            </th>
            <th>
                #Название поля
            </th>
        </tr>
        {% for key, i in builder %}
            <tr>
                <td>
                    {{ letters[key] }}
                </td>
                <td>
                    {{ i }}
                </td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}